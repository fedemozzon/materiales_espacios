version: "3.9"

services:
  db:
    image: postgres:14
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5432:5432"
  backend:
    build:
      context: ./api_materiales
      dockerfile: Dockerfile
    command: python ./api_materiales/manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    environment:
      - POSTGRES_NAME=dssdapi
      - POSTGRES_USER=grupo9
      - POSTGRES_PASSWORD=7HeG2Ws9NWGzT3DCmWTrSFBl12ryScFa
      - POSTGRES_HOST=dpg-cd8tm702i3mol837cq9g-a.oregon-postgres.render.com
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - db
